---
import data from "../../src/assets/lastEvents/data.json"

import BaseLayout from "../layouts/BaseLayout.astro";

import ImgTextSection from "../components/astro/ImgTextSection.astro";

import { CustomCarrusel } from "../components/react/CustomCarrusel"; 
---

<BaseLayout title="Eventos Pasados">
  <main>
    <section class="banner">
      <CustomCarrusel client:visible />
      <div class="baner-gradient"></div>
      
      <div class="banner-text">
        <h1>Imaginatios Anteriores</h1>
        <p>
          Esta sección reúne las ediciones anteriores de <strong>Imaginatio.</strong> Aquí podrás descubrir las temáticas, actividades, invitados y experiencias que han marcado cada edición.
        </p>
      </div>
    </section>

    <section class="events">
      {data.map(event => 
        <ImgTextSection
          image={event.image}
          text={event.text} 
          version={event.version}
          width={event.width}
          height={event.height}
        />
      )}
    </section>
  </main>
</BaseLayout>

<style>
  .events {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    gap: 10px;
    padding: 10px;
  }

  .banner {
    position: relative;
    width: 100%;
    height: 500px;
    margin: 20px 0;
    overflow: hidden;
    background: black;
  }

  .baner-gradient{
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: #020024;
    background: linear-gradient(
      45deg, #0d00ff 0%, #ff00d4 100%
      );
      opacity: 85%;
  }

  .banner-text {
    z-index: 10;
    position: absolute;
    inset: 0;
    display: flex;
    flex-direction: column;
    align-items: start;
    justify-content: end;
    gap: 20px;
    padding: 50px;
    max-width: 700px;
    pointer-events: none;
    font-size: 1.5em;
  }

  .banner-text a {
    padding: 10px 20px;
    border-radius: 10px;
    text-decoration: none;
    color: wheat;
    background-color: #ff00d4;
    pointer-events: all;
  }

  .banner-text a:hover {
    cursor: pointer;
    transform: scale(1.1);
  }

  @media screen and (max-width:500px) {
    .banner-text {
      padding: 15px;
      font-size: 1.2em;
    }
  }
</style>